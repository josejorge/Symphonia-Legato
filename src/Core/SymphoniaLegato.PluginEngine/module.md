# Module: SymphoniaLegato.PluginEngine

**Path:** `src/Core/SymphoniaLegato.PluginEngine/`
**Type:** Core library (engine layer)

## Purpose
Plugin host and sandbox for third-party extensions (instruments, exporters).
Surfaced in the Desktop app's Plugin Manager dialog.

## Depends on
`SymphoniaLegato.Core`.

## Depended on by
`Desktop`.

## Key files
See `docs/PLUGIN_SDK.md` at the project root for the plugin authoring contract
this module implements.

## Docs
`docs/operations_guide.html`, `docs/executive_overview.html` (this module).
Full architecture: root `CLAUDE.md` / `docs/ARCHITECTURE.md` /
`docs/PLUGIN_SDK.md`.
